import { SafeAreaView, Text } from "react-native";

export default function AppointmentsComponent() {
    return (
        <SafeAreaView>
            <Text>Appointment Doctor Component</Text>
        </SafeAreaView>
    )
}